volumes:
  db-data:

networks:
  micro-net:
    driver: bridge

services:
  web-hextris:
    image: jatisatrio/web-hextris
    build: ./web-hextris
    ports:
      - 6000:80
    networks:
      - micro-net